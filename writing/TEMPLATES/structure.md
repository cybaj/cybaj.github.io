---
sections: {}
---

## Notes

<!-- editing/{lang}/ 의 디렉터리가 곧 섹션이다. 여기에는 디렉터리 이름만으로
     알 수 없는 것만 적는다: 언어별 제목, weight, hugo-book 플래그.

     고칠 섹션만 적으면 된다. 적지 않은 섹션은 디렉터리 이름을 제목으로 쓰고
     publish 가 경고한다. 루트는 publish.md 의 title 을 쓴다.

sections:
  templates:
    title: {ko: "템플릿", en: "Templates"}
    weight: 20
    bookCollapseSection: true
-->
